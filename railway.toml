[phases.build]
cmds = [
  "cd apps/api",
  "npm install",
  "npx prisma generate --schema=../../prisma/schema.prisma",
  "npm run build"
]

[phases.setup]
nixPkgs = ['nodejs', 'npm']

[start]
cmd = "cd apps/api && npm run start:prod"
